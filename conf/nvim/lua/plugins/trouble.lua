return {
	"folke/trouble.nvim",
	opts = {
		auto_open = true,
		auto_close = true,
		pinned = true
	}, -- for default options, refer to the configuration section for custom setup.
	cmd = "Trouble",
	keys = {
	},
}
